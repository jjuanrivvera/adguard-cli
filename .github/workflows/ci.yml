name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  # auto (not local) so Go can fetch the toolchain the go.mod directive requires
  GOTOOLCHAIN: auto

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: 'stable'
      - name: Build
        run: go build -o /dev/null ./cmd/adguard-home/
      - name: Test
        run: go test -race -v ./...

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: 'stable'
      - uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12.2
